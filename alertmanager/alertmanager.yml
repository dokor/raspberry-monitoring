global:
  resolve_timeout: 5m

route:
  receiver: "default-receiver"
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 3h

receivers:
  - name: "default-receiver"
    # Exemple d'email (à adapter si tu veux utiliser SMTP avec un SMTP relay)
    # email_configs:
    #   - to: "toi@example.com"
    #     from: "alertmanager@example.com"
    #     smarthost: "smtp.example.com:587"
    #     auth_username: "user"
    #     auth_password: "pass"

    # Exemple de webhook (Discord, Slack via webhook, etc.)
    # webhook_configs:
    #   - url: "https://url-de-ton-webhook"
